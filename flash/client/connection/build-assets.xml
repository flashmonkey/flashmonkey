<project name="flash.connection.assets">
    <dirname property="flash.connection.assets.dir" file="${ant.file.flash.connection.assets}"/>

    <!-- Define SWC exported by this library. -->
    <property name="flash.connection.swc" location="${flash.connection.assets.dir}/${build.compc.dir}/flashmonkey-connection.swc"/>

</project>
